<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.theater.mapper.TicketMapper">

<select id="movieList" resultType="com.theater.domain.MovieVO"> <!-- 예매화면 영화 리스트 -->
  SELECT * FROM MOVIES
</select>


<select id="getAreaSelect" resultType="com.theater.domain.ReserveVO"> <!-- 예매화면 영화 리스트 -->
  SELECT
    DISTINCT(t.t_area),f.m_cd
    FROM FILMS F, SCREENS S, THEATERS T
    WHERE F.SCREEN_CD = S.SCREEN_CD AND S.T_CD = T.T_CD
    AND F.M_CD = #{m_cd}
</select>

<select id="getTheaterSelect" resultType="com.theater.domain.ReserveVO"> <!-- 예매화면 영화 리스트 -->
  SELECT
    t.t_area,f.m_cd,t.t_name,t.t_cd
    FROM FILMS F, SCREENS S, THEATERS T
    WHERE F.SCREEN_CD = S.SCREEN_CD AND S.T_CD = T.T_CD
    AND F.M_CD = #{m_cd}
    AND T.T_AREA = #{t_area}
</select>
</mapper>
